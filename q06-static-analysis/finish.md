## Lab Complete! 🎉

### Key points
- Constraint '1 line only' = modify the existing value, never add a new block
- `USER nobody` = UID 65535 = no privileges on the host
- `readOnlyRootFilesystem: true` = container cannot write binaries to its filesystem
- Use `sed -i` for precise 1-line changes under exam time pressure
