## Lab Complete! 🎉

### Key tips for the exam
- `kubectl create secret tls` requires `--cert` and `--key` flags
- Files inside the mount are always `tls.crt` and `tls.key`
- Always `readOnly: true` for TLS mounts
- This is a **free marks** question — 4-5 min max